# Main Contrast Pre-registration (Redacted Public Copy)

Version: v1.3 redacted for anonymous review.

The analysis unit is essay by trait. For each cell, compute A(i,t)=J(F_SHAP(i,t), F_LLM(i,t)). If both sets are empty, A(i,t)=0.

Primary hypothesis: Surface alignment is greater than Content alignment under the pre-registered form-family operational metric.
