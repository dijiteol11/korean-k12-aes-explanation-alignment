# Data License and Restrictions

The aggregate derived tables in `data/derived_public/` may be used for scholarly review and non-commercial reproducibility checks with attribution to the associated manuscript.

The raw Korean student-essay corpus is not redistributed. Users who need raw-text replication must obtain the corpus independently and configure local paths using `configs/paths_template.py`.
